{
  "name": "deliverer-e2e-tests",
  "version": "0.1.0",
  "private": true,
  "description": "The end-to-end harness: it installs the plugin the way a user does and drives whole runs against it. Nothing here ships — only plugin/ is published — and CI typechecks and lints it without ever running it.",
  "type": "module",
  "engines": {
    "node": "^22.18.0 || >=23.6.0"
  },
  "scripts": {
    "test": "node --test",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.3",
    "eslint": "^10.8.1",
    "globals": "^17.9.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.66.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.220"
  }
}
