# 01 — Say what `plugin/mcp/` holds, before it holds more than a server

Status: ready-for-agent

**Blocked by:** None — can start immediately.

**What to build:** a contributor reading the project tree, the project instructions or the package's own description
finds `plugin/mcp/` described as the plugin's Node code, of which the tools server is one part — rather than as the
tools server itself. Every ticket after this one adds a second entry point to that directory, and each of them would
otherwise have to correct the same three documents on its way past. **Prefactoring**: it lands first so no later ticket
leaves a document lying. Settled as D30 and D31 in `../spec.md`.

- [ ] CONTRIBUTING's project tree describes `plugin/mcp/` as the plugin's Node code, and names the tools server as
      what it holds today rather than as what the directory is.
- [ ] CONTRIBUTING says it outside the project tree too: § CI introduces the two packages it checks, and the one in
      `plugin/mcp` is named there as the tools server. That is the same claim D31 retires, in the file this ticket
      already has open.
- [ ] `.claude/CLAUDE.md`'s section on the directory is reworded the same way, and every claim it already makes
      survives intact: that the code ships unbuilt, that `tsc --noEmit` is the only thing holding up the three
      options that allow it, and that the comments in `server/` carry the reasoning.
- [ ] The label beside the first of the two commands `.claude/CLAUDE.md` lists as the whole of what is checked is
      reworded with it, so the file does not name the package one way where it lists the checks and another way
      where it describes the directory.
- [ ] The package's own `description` no longer says the directory is only an MCP server.
- [ ] The package `name` is left alone. Renaming it churns the lockfile and the install for a word, and nothing
      outside this repository reads it.
- [ ] `.github/workflows/ci.yml` is left alone, though its job and step names say the retired thing as well. Those
      names label what each step checks and a run reports green under any of them; correcting them is a separate
      pass rather than this ticket's.
- [ ] No file moves, no import changes, and nothing about what CI runs changes: both commands, their working
      directories and the guard that stops one failure hiding another are untouched. A comment that labels a
      command is prose, and the criteria above have it.
- [ ] The project tree's count of `docs/adrs/` is corrected in passing — it says fifteen where the directory holds
      eighteen. A figure nobody maintains is the same lying document this ticket exists to end, and it sits in the
      block already being edited.
- [ ] The framing is durable: adding a second entry point later must not require rewriting these sentences again,
      only extending the list of what the directory holds.
- [ ] No ADR is added — this is settled in the spec, not architecture in its own right.
- [ ] The glossary's words are used, and each file's prevailing column width and register are matched.

## Comments

> *This was generated by AI during triage.*

**Triage, 2026-08-26 — stays `ready-for-agent`; four criteria added, one reworded.**

Nothing here is built. All three documents still say the thing D31 retires: the project tree's line for the directory
calls it the tools server outright, `.claude/CLAUDE.md`'s section on it is titled that way, and the package's
`description` opens "MCP server the deliverer plugin ships". The search was by concept rather than by the request's
wording — every "tools server" in the markdown, the workflow, the manifests and the hook outside `docs/specs/`, plus
the project tree and the ADRs. No prior rejection of this shape is on record; the repository keeps no `.out-of-scope/`.

Both of the body's standing claims hold at the code. `deliverer-tools-server` appears in `plugin/mcp/package.json` and
its lockfile and nowhere else — the package is `private`, and `.mcp.json`, the install hook and the launcher each reach
for a path rather than the name — so leaving the name alone costs nothing that is visible anywhere. And no ADR is owed:
the spec settles the framing in D30 and D31, and its non-goals already decline the honest rename as the **wide
refactor** it would be across the manifest, the hook, the launcher, CI and two ADRs.

What triage added are sites the checklist did not reach, each of which the ticket that exists to stop a lying document
would have left lying. CONTRIBUTING says the retired thing outside the project tree, where § CI introduces the two
packages it checks and names the one in `plugin/mcp` as the tools server. `.claude/CLAUDE.md` labels the first of the
two commands it lists as the whole of what is checked the same way, which would have left the file naming the package
one way on that line and another in its reworded section; the criterion guarding CI read "the two commands CI runs are
untouched", which an agent can fairly read as forbidding that edit, so it now guards what the commands run and says a
label is prose. And the tree's count of `docs/adrs/` says fifteen where the directory holds eighteen — a figure nobody
maintains, sitting in the block the agent already has open.

Two things were weighed and left out. `.github/workflows/ci.yml` says the retired thing in its job name, three step
names and the job's own comment, and a criterion now records that it is deliberately untouched: those names label what
a step checks, and a run reports green under any of them. Ticket 04 gained a criterion instead of this one growing —
the durability criterion here promises a later ticket only extends the list of what the directory holds, and no ticket
in the epic had that extension on it, so both project-tree lines 04 changes are now named on 04's own file.
