# 09 — The parts CI cannot reach have a procedure

Status: ready-for-agent

**Blocked by:** 06, 07, 08

**What to build:** a contributor who changes the **observer** knows what to run before calling it done. Nothing here is
covered by CI and only the shallowest part of it is covered by a paid test, which is exactly the position the tools
server is already in — so this is the section that does for observation what § Exercising the install by hand does for
the launcher. Settled under Testing Decisions in `../spec.md`.

**This is the completion pass, not the first draft.** Two earlier tickets each land part of the procedure as they go,
and neither can finish it. Ticket 03 writes the replay procedure into § What CI does not check in the only form that
exists while nothing judges. Ticket 04 walks the six hook states once, to report. By the time both are done the feature
judges every **dispatch**, writes **dispatch note**s and reads an **epic**'s earlier **debrief**s — none of which the
text left behind mentions, and none of which a contributor has been told to exercise. This ticket finishes what those
two started and makes the claims around it true again. Every criterion below extends or corrects existing text; none of
it rewrites what another ticket landed.

- [x] § What CI does not check names the hook states worth walking as a standing procedure, each with what it should
      do: a session with no run in it, a `/deliverer:` command typed, a run resumed by prose, a session ended mid-run,
      a killed terminal, and observation switched off. Ticket 04 walks exactly this list once and reports what it saw;
      this is the list written down, so the next contributor to touch the hook walks it without re-deriving it.
- [x] The replay procedure is complete in both forms. Ticket 03 lands the one that needs no model — free, deterministic
      and the only form there is while nothing judges — and this adds the form with judging on, which costs up to
      thirteen model calls for one run's **trace** and is what actually exercises the judgement. It says which of the
      two answers which question: the free one that the deterministic half still holds, the paid one whether the
      observer finds what a human found by hand.
- [x] It says which artifacts to read afterwards and what each one should contain, complete for the feature as it now
      stands: the **debrief** and its header, the **trace** with its refusals, the **dispatch note**s ticket 06 writes,
      the **identity file**, and the earlier debriefs of the same **slug** that ticket 07 makes an observer read. The
      last three exist in no procedure before this ticket.
- [x] It states plainly that the observer is verified by hand and that CI runs none of it — and that the one automated
      check standing above it is the shallow assertion ticket 08 adds to the two paid tests, which says a debrief
      appeared and nothing about whether it is right.
- [x] Nothing claims a test runner, a fixture or a test suite covers the observer. **CONTRIBUTING's own first bullet
      under that heading is left standing**: "The tests exist and CI runs none of them." It replaced an older "There is
      no test suite" precisely to lose a falsehood while keeping its point, and nothing here puts that back.
- [x] The list of what CI does not check is accurate again once observation exists. It currently names the server, the
      launcher and the SessionStart hook; observation joins them as a **second** hook, so that bullet's singular goes
      with it.
- [x] The count-dependent claims elsewhere in the file are true again, each of which this epic moves and no other
      ticket reaches: "the two procedures" in both places it appears — § What CI does not check and § The end-to-end
      tests — once replay makes three; "any change to a manifest, the hook or the launcher" in the smoke test's line;
      and "the plugin's three options at user scope" in what a run leaves behind, which ticket 04's criterion does not
      cover because that one is about the project tree.
- [x] This ticket's files are `CONTRIBUTING.md` and nothing else. The README's disclosure is ticket 04's,
      `e2e-tests/README.md`'s is ticket 08's, the project tree's two lines are ticket 04's, and no source, manifest or
      glossary entry is touched here.

## Comments

> *This was generated by AI during triage.*

**Triage, 2026-08-26 — stays `ready-for-agent`; reframed as a completion pass, two criteria added, six rewritten, one
dependency added, none removed.**

Nothing here is built. The search was by concept rather than by the request's wording — `debrief`, `observer`,
`observation`, `replay`, `distill`, `dispatch note` across every source, manifest, hook and document outside
`docs/specs/` — and the only hits are `CONTEXT.md`'s own glossary entries plus `e2e-tests/harness/session.ts`'s
`McpServerObservation`, which is the harness's unrelated vocabulary. No prior rejection of this shape is on record; the
repository keeps no `.out-of-scope/`. Every section this ticket edits exists and was read: § What CI does not check
(`CONTRIBUTING.md:241`), § Exercising the install by hand (`:272`) and § The end-to-end tests (`:293`).

**This was the only ticket in the epic that had not been through a triage.** Its siblings were all sharpened a day
later; this one still carried its `to-tickets` draft, and two of its six criteria had since been overtaken by the
tickets that came before it.

**The overlap was the reason to reframe it.** Ticket 03 already carries "CONTRIBUTING § What CI does not check gains the
replay procedure beside the scripted backend's: the command, what it needs, and what to read in what it leaves behind"
(`03:94`), and ticket 04 already carries "The hook states are walked by hand and reported" with the identical six states
(`04:83`). Read as a first draft, this ticket had an implementer writing a procedure that exists and re-deriving a list
that had already been walked. The two are genuinely different jobs and the body now says so: 04 walks the states *once*
to report, this writes them down as a *standing* procedure; and 03's replay form is the only one that can exist while
nothing judges, since judging arrives in 05 and dispatch notes in 06, so this ticket's job is the second form and the
artifacts the later tickets add. The criteria now name what already exists, which is what keeps this an extension rather
than a rewrite of two other tickets' work.

**One criterion would have undone an earlier fix.** "Nothing anywhere claims this repository has a test suite"
was true when the epic was drafted and is now a hazard: the end-to-end-tests epic deliberately replaced CONTRIBUTING's
"**There is no test suite.** `package.json` has exactly two scripts" with "**The tests exist and CI runs none of
them.**" in order to lose the falsehood while keeping the point — that epic's own ticket 04 records the reasoning. Taken
literally, this ticket asked an implementer to undo that. It now bars the claim it meant to bar — a test runner or
fixture covering the observer — and quotes the sentence that must survive.

**One criterion was wrong about what a paid replay costs.** It said judging on "costs one call". A **dispatch note** is
written per dispatch, so a replay of a delivery calls a model once per dispatch — up to thirteen times, which is what
`03:73` and `../spec.md:378` both put it at. Left as it was, the procedure would have told a contributor to expect a
single call and priced the exercise an order of magnitude under. The criterion now carries the real figure, which is
also what makes it the form worth running deliberately rather than routinely.

**Ticket 08 became a dependency.** The body claims only the shallowest part of observation is covered by a paid test,
and that assertion is what ticket 08 builds. Landing this first would have documented a coverage the harness does not
have. `06, 07` became `06, 07, 08`, and a criterion now names what 08's assertion does and does not settle, so the
sentence is grounded rather than asserted.

**The accuracy sweep was one bullet wide and is now five claims.** The original criterion covered only the three-bullet
list of what CI does not check. Four more claims in the same file are count-dependent and this epic moves every one:
`:251` names "the SessionStart hook" in the singular where D22 adds a prompt-level second one; `:254` and `:297` both
say "the two procedures" where replay makes three; `:302` repeats the singular hook in the smoke test's line; and `:368`
says "the plugin's three options at user scope" where ticket 04 adds a fourth. That last one is the one worth calling
out: ticket 04 *does* carry an option-count criterion, but it is scoped to CONTRIBUTING's project tree, so `:368` in
§ The end-to-end tests is reached by nothing in the epic. A closing criterion scopes this ticket to one file, because
most of its criteria now correct prose that other tickets also touch.

**Two findings were weighed and declined here, recorded so they are not lost.**

`replay` **has no glossary entry.** It is bold as a domain term at `03:8` ("This is **replay**"), it is D24's whole
subject, and it is what this ticket's second criterion is about — but `CONTEXT.md` defines it in neither § Observation
nor § Verification. Under `.claude/CLAUDE.md`'s rule a needed term that is missing is a gap rather than invented
language, and ticket 08's triage fixed exactly this shape inline by adding **Ceiling**. The maintainer declined it for
this ticket, and the closing criterion's bar on touching the glossary is that decision. The gap stands and wants a home.

**CONTRIBUTING's ADR count is stale.** `:142` reads "architectural decisions — fifteen, all on the plugin itself"
against eighteen files on disk. It was already wrong by one before this epic (0016 arrived with the end-to-end tests)
and this epic makes it wrong by three, having added 0017 and 0018. Declined here: it is not a claim observation moves,
and it is wrong today rather than made wrong by this work.
