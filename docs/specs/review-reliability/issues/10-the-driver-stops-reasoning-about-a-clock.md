# 10 — Stop `code-reviewer` reasoning about a clock it cannot read

Status: ready-for-agent

**Blocked by:** None — can start immediately.

**What to build:** `code-reviewer` stops pacing itself with a timer it does not control, and the server stops handing it
an elapsed figure it has no use for. Its `sleep` is killed at the Bash tool's two minutes and it is then not rescheduled
for 17 to 45, so an observed run reasoned aloud about "the 60-minute deadline" while its own clock was three quarters of
an hour out, and one finished round sat unnoticed for 17 minutes by the one agent whose whole job was to notice. It also
slept two minutes against a shipped instruction of fifteen seconds, for the second run running. Settled as D19 and D20
in `../spec.md`.

- [ ] `code-reviewer` no longer sleeps — in **either** place it currently does: step 4's poll loop, and step 3's branch
      for a review already in flight. What replaces both is that it calls `code_review_status` again and repeats, and
      nothing is said about when that next call happens, because nothing the agent can do decides it.
- [ ] No sentence in it implies that it controls its cadence, or that it can measure elapsed time or a deadline. It is
      not told it has wake-ups either: it has no scheduling facility, and an agent told it has one invents a use for it
      — which is the defect this ticket exists to fix rather than a way to fix it.
- [ ] Step 3's handle branch keeps only the `review_id`. `poll_after_ms` stops being the agent's to hold: with the
      pacing gone it has no stated use, and a number an agent holds for no stated reason is one it invents a use for.
- [ ] That nothing arrives unsolicited, and that the status tool is the only thing bearing a result, are unchanged. So
      is the reassurance that the review reaches a terminal status by itself, bounded by the server's own deadline —
      that is what makes a poll loop with no interval safe to state.
- [ ] The server keeps publishing `poll_after_ms`, per D20: the hint is advice for any caller and not just the shipped
      one. The comment claiming it is kept in step by hand with a shipped `sleep` is gone, because after this ticket
      there is no such `sleep` for it to be in step with. The measurement behind 15 s — a healthy round runs ~122 s, so
      ~8 polls — survives as the grounds for the number.
- [ ] `stats.durationMs` leaves the status payload, and both descriptions that lean on it go with it: the `events`
      description stops naming it and rests on `lastEventAt`, and `agentDurationMs`'s stops contrasting against it.
      This costs no diagnostic and contradicts nothing already written — the field's own comment says it "rises either
      way and answers neither", and `lastEventAt`'s says that field and `events` are together what tells a poller
      working from wedged. Two polls with the same `events` and the same `lastEventAt` say nothing has happened since
      the last one, and need no clock to read.
- [ ] `stats.deadlineSec` stays. Ticket 04 makes its description the only place a caller can learn the idle bound
      exists before a round ends on it, so dropping the key would leave that ticket nothing to write to. The two
      tickets edit different keys of the same `stats` object and land in either order.
- [ ] The honest limit is stated rather than implied: the payload still carries the deadline, and `startedAt` still
      dates the review's start, so this change removes every instruction to reason about a clock without removing every
      means of building one. What it buys is that nothing tells `code-reviewer` those numbers are its to act on.
- [ ] What `code-reviewer` reports is otherwise unchanged — the `review_id`, the `status` it ended on, the verbatim
      `summary`, what the round **spent**, and the one-line `reason` on a round that failed or was cancelled.
- [ ] The review lifecycle is exercised against the **scripted backend** — the payload shape moves, and nothing in CI
      looks at it — and `typecheck` and `lint` pass in both packages. The register and column width of each file are
      matched.

## Comments

> *This was generated by AI during triage.*

**Triage, 2026-08-23 — stays `ready-for-agent`; four questions settled, one of which reversed itself on ticket 04.**

**The claim verifies, and every figure in it has a source.** `sleep 15` is present twice in
`plugin/agents/code-reviewer.md` — step 3's in-flight branch and step 4's poll loop — `POLL_AFTER_MS = 15_000` is
published as `poll_after_ms`, and `lifecycle.ts` carries the pairing comment verbatim: *"It must equal the interval the
SHIPPED agent sleeps for … kept in step by hand."* `DEADLINE_SEC = 60 * 60`, so the "60-minute deadline" the observed
run quoted is a real number rather than an invented one. The 17-to-45-minute rescheduling, the 17-minute unnoticed
round, and the two-minute sleep against a shipped fifteen seconds are all in `../spec.md`. Nothing in `plugin/`
implements any of it — searched by concept (`sleep|poll|cadence|interval|deadline|elapsed|wake`) rather than by the
request's wording — and no `.out-of-scope/` exists, so there is no prior rejection to weigh.

**The ticket would have had an implementer write a false mechanism into a shipped contract.** Criterion 1 promised that
what the agent does instead is stated: *"it polls on its own wake-ups."* That phrase appears nowhere in `../spec.md` —
D19 says only that the sleeping and the reasoning about elapsed time stop, and names no replacement — and a dispatched
agent has no scheduling facility to wake itself with. Telling one that it does is D19's own failure mode restated: *"an
agent told it has a clock invents one."* On your call the criterion is a pure deletion plus the one true positive
statement — call the tool again and repeat — with the existing sentence about the server's deadline left in place, since
that is what makes an interval-free loop safe to state at all.

**Prose deletions alone would not have removed the clock, and the fix for that was reversed by a sibling ticket.** The
status payload publishes `stats.durationMs` and `stats.deadlineSec` on every poll, each with a schema description
telling the caller what it is — so criterion 2 was satisfiable while the agent still read both numbers every fifteen
seconds, and the payload is the likelier source of the observed "60-minute deadline" reasoning than the contract ever
was. You first widened scope to drop both. Checking the collision surface afterwards found that **ticket 04 requires
`deadlineSec` to stay**: two of its `ready-for-agent` criteria turn on it, one of them explicitly — *"D8 keeps the idle
bound out of the payload, so this description is the only place a caller can learn it exists before a round ends on
it."* Dropping the key would have made both unsatisfiable and broken D25's promise that these land in any order. So
`deadlineSec` stays with the limit stated honestly, and only `durationMs` goes.

**Removing `durationMs` settles a contradiction already in the source rather than creating one.** Two descriptions
currently both claim to complete the working-versus-wedged signal: `events` says *"two polls with the same number and a
moving `durationMs` mean nothing has happened since the last one"*, while `lastEventAt` says *"With `events`, this is
the whole of what says a live review is working rather than wedged."* The field's own comment sides with the second —
`durationMs` *"rises either way and answers neither"*. So the pairing the `events` description names is the weaker of
the two and the only one that needs a clock; `events` plus `lastEventAt` compare one poll against the next and need
none. `agentDurationMs` stays: it is a fact about what the round cost, not a clock, and it survives a `failed` status
by design. Its description is the third edit, since it currently defines itself against `durationMs` by name.

**Two sleeps, not one, and a number left in the agent's hands.** Criterion 1 said "between polls", which is step 4 only;
step 3's in-flight branch also says `sleep 15` and would have survived a literally-read checklist. Both are named now.
Step 3 also said *"Keep its `review_id` and its `poll_after_ms`"* — after D19 that leaves the agent holding an interval
with no stated use, which is the shape of the failure rather than a leftover. On your call it keeps the `review_id`
alone, which also ends a mismatch `lifecycle.ts` already confesses: *"the role never read the hint (grill A6)."*

**Scope checked in three directions.** No test in `plugin/mcp` asserts on the payload — there are none outside
`node_modules`. `e2e-tests`' `durationMs` hits are `outcome.run.durationMs`, the harness's own wall-clock for a whole
run, and have nothing to do with the server's status result; an implementer should not chase them. Of the sibling
tickets only 04 touches the `stats` object, on a different key. Ticket 02 also writes to `code-reviewer.md`, but to the
intro's posting claim rather than to steps 3 and 4, so the two land in either order.

**The glossary displaces "driver", and the ticket now says `code-reviewer`.** `CONTEXT.md`'s **Orchestrator** entry
lists `_Avoid_: coordinator, driver, manager`, and this ticket called `code-reviewer` "the driver" throughout —
including in its title. The role already has a name, so on your call the ticket uses it and no glossary work follows.
Two things are recorded rather than actioned: `../spec.md` uses "driver" in its problem statement and in D19 itself,
which is prose already written and not this ticket's to rewrite; and the filename still reads `10-the-driver-…`, left
alone because nothing references it and renaming a `ready-for-agent` ticket's path buys less than it risks.

**No ADR.** A contract sentence and a payload key are both trivially reversible, and D19 and D20 already hold the
decision in the one place it lives.
